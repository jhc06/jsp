변수(variable)는 특정 개체에 속해 있지 않다.
보통 키(key)와 값(value)으로 구성되어있으며, 그 중 키는 값에 접근할 수 있는 식별자 역할을 한다.
반면, 프로퍼티(property)는 특정 개체에 속해 있으며 개체 속 변수를 뜻하는 말이다.

함수(function)는 특정 개체에 속해 있지 않다.
반면, 메소드(method)는 특정 개체에 속해있는 함수를 뜻하는 말이다.